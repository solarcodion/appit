type MENU_LINK = {
  title: string;
  link: string;
};

type ANALYZE_DATA = {
  title: string;
  desc: string;
  from: number;
  to: number;
};

type FEATURE_DATA = {
  img: any;
  icon: any;
  title: string;
  desc: string;
};

export type { MENU_LINK, ANALYZE_DATA, FEATURE_DATA };
